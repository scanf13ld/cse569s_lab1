# How to run
## Install dependencies
```
pip install opencv-python
pip install pillow
pip install matplotlib
pip install ckwrap
pip install bcrypt
```

## Run
`python main.py`

python version used is 3.10.2